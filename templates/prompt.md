---
id: {prompt_id}
version: 0.1.0
owner: {consumer_repo}
model_family: gpt-4o-mini
schema: {SchemaId}
eval: evals/{prompt_id}.jsonl
status: experimental
---

# {prompt_id}

{Instrucciones del prompt. El frontmatter es obligatorio desde la semana 2;
desde la semana 5, un prompt de producción sin eval no está terminado (D6.1).}